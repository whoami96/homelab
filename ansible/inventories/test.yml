all:
  children:
    linux:
      hosts:
        cloudflared:
          ansible_host: 10.0.0.6
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/dev_keys/homelab/ansible
          ansible_port: 22
