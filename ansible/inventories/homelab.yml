all:
  children:
    homelab:
      hosts:
        pve:
          ansible_host: 10.0.0.2
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/keys/homelab/ansible
        docker:
          ansible_host: 10.0.0.3
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/keys/homelab/ansible
        nexus:
          ansible_host: 10.0.0.4
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/keys/homelab/ansible
        cloudflared:
          ansible_host: 10.0.0.5
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/keys/homelab/ansible