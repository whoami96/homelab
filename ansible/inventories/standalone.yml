all:
  children:
    docker:
      hosts:
        linux:
          ansible_host: 10.0.0.10
          ansible_user: whoami
          ansible_ssh_private_key_file: ~/.ssh/dev_keys/homelab/whoami
          ansible_port: 22