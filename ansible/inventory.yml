all:
  children:
    Docker:
      hosts:
        docker-prod:
          ansible_host: 10.0.0.3
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/homelab/docker-prod
          compose_file_paths: 
            - ../docker-compose/docker-prod/portainer.yaml
