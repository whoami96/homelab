name: transmission

services:
  transmission:
    image: linuxserver/transmission:4.0.6
    container_name: transmission
    environment:
      - PUID=1001
      - PGID=1001
      - TZ=Europe/Warsaw
    volumes:
      - /home/whoami/docker/volumes/transmission/config:/config
      - /mnt/download:/downloads
      - /home/whoami/docker/volumes/transmission/watch:/watch
    ports:
      - 9091:9091
      - 51413:51413
      - 51413:51413/udp
    restart: unless-stopped
